{% extends '::base.html.twig' %}

{% block title %}Texte im HGV{% endblock %}

{% block body %}
  <h5>Texte im HGV <a href="http://www.rzuser.uni-heidelberg.de/%7Egv0/Texte/HGV-Texte.html" title="Link zur Originaldatei" onfocus="this.blur()" target="_blank"> → Quelle</a></h5>
  <div id="publication">
    <ul id="volume">
      {% for publication in publications %}
        <li data-url="{{ path('PapyrillioHgvBundle_PublicationLoadNumbers', {'collection': publication.collection, 'volume': publication.volume, 'particle': publication.particle}) }}">
          {% if publication.totalReprint %}
            <i>
          {% endif %}
          {{ publication.collection }} {{ publication.volume }}{{ publication.particle }}
          {% if publication.totalReprint %}
            </i>
          {% endif %}
        </li>
      {% endfor %}
    </ul>
    <ul id="number"></ul>
    <ul id="result"></ul>
  </div>

{% endblock %}

{% block javascripts %}
  {{ parent() }}
  <script src="{{ asset('js/publication.js') }}" type="text/javascript"></script>
{% endblock %}