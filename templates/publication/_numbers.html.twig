{% for pub in numbers %}
  <li data-url="{{ path('PapyrillioHgvBundle_PublicationLoadRecords', {series: pub.targetSeries, volume: pub.targetVolume, number: pub.targetNumber}) }}">
    {{ pub.label }}
  </li>
{% else %}
  <li class="placeholder">Keine Nummern gefunden.</li>
{% endfor %}
